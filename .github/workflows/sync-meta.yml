# 文件名: .github/workflows/sync-meta.yml
# 日常使用的同步工作流 - 与源仓库完全一致

name: Sync Meta Folders Only

on:
  schedule:
    - cron: '30 18 * * *'  # UTC 18:30 = 北京时间第二天 2:30
    - cron: '30 6 * * *'   # UTC 6:30 = 北京时间 14:30
  workflow_dispatch:
    inputs:
      force_sync:
        description: '强制同步（即使哈希无变化）'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0  # 获取完整历史以便比较

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create or ensure meta directory
        run: |
          mkdir -p meta
          echo "确保 meta 目录存在"

      - name: Clone source repository
        id: clone_source
        run: |
          SOURCE_DIR="/tmp/meta-source-$(date +%s)"
          echo "source_dir=$SOURCE_DIR" >> $GITHUB_OUTPUT
          
          echo "克隆源仓库 meta 分支..."
          git clone --depth=1 --branch=meta https://github.com/MetaCubeX/meta-rules-dat.git "$SOURCE_DIR"
          
          echo "源仓库结构:"
          ls -la "$SOURCE_DIR/"

      - name: Get current meta status
        id: current_status
        run: |
          echo "检查当前 meta 目录状态..."
          
          # 检查当前 meta 目录是否存在并获取基本信息
          if [ -d "meta" ] && [ "$(ls -A meta 2>/dev/null)" ]; then
            # 计算当前文件的MD5哈希（仅用于比较）
            echo "计算当前 meta 目录的哈希摘要..."
            find meta -type f -name "*" ! -name ".*" -exec md5sum {} \; | \
              sort | md5sum | cut -d' ' -f1 > /tmp/current_hash.txt
            
            CURRENT_HASH=$(cat /tmp/current_hash.txt)
            echo "当前 meta 哈希摘要: $CURRENT_HASH"
            echo "current_hash=$CURRENT_HASH" >> $GITHUB_OUTPUT
            
            # 统计文件数量
            FILE_COUNT=$(find meta -type f 2>/dev/null | wc -l)
            echo "current_file_count=$FILE_COUNT" >> $GITHUB_OUTPUT
            echo "当前有 $FILE_COUNT 个文件"
          else
            echo "当前 meta 目录为空"
            echo "current_hash=empty" >> $GITHUB_OUTPUT
            echo "current_file_count=0" >> $GITHUB_OUTPUT
          fi

      - name: Get source repository status
        id: source_status
        run: |
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          echo "计算源仓库的哈希摘要..."
          
          # 计算源仓库文件的MD5哈希（仅用于比较）
          find "$SOURCE_DIR" -type f -name "*" ! -name ".*" ! -path "*/.git/*" -exec md5sum {} \; | \
            sort | md5sum | cut -d' ' -f1 > /tmp/source_hash.txt
          
          SOURCE_HASH=$(cat /tmp/source_hash.txt)
          echo "源仓库哈希摘要: $SOURCE_HASH"
          echo "source_hash=$SOURCE_HASH" >> $GITHUB_OUTPUT
          
          # 统计源仓库文件数量
          SOURCE_FILE_COUNT=$(find "$SOURCE_DIR" -type f ! -path "*/.git/*" 2>/dev/null | wc -l)
          echo "source_file_count=$SOURCE_FILE_COUNT" >> $GITHUB_OUTPUT
          echo "源仓库有 $SOURCE_FILE_COUNT 个文件"

      - name: Compare and decide sync
        id: decide_sync
        run: |
          CURRENT_HASH="${{ steps.current_status.outputs.current_hash }}"
          SOURCE_HASH="${{ steps.source_status.outputs.source_hash }}"
          
          echo "哈希比较结果:"
          echo "当前哈希: $CURRENT_HASH"
          echo "源哈希: $SOURCE_HASH"
          
          # 决定是否需要同步
          if [ "${{ github.event.inputs.force_sync }}" = "true" ]; then
            echo "用户选择了强制同步（即使哈希无变化），将执行同步"
            echo "need_sync=true" >> $GITHUB_OUTPUT
            echo "sync_reason=force_sync_user_request" >> $GITHUB_OUTPUT
          elif [ "$CURRENT_HASH" = "empty" ]; then
            echo "当前 meta 目录为空，需要同步"
            echo "need_sync=true" >> $GITHUB_OUTPUT
            echo "sync_reason=empty_directory" >> $GITHUB_OUTPUT
          elif [ "$CURRENT_HASH" = "$SOURCE_HASH" ]; then
            echo "哈希相同，内容没有变化，无需同步"
            echo "need_sync=false" >> $GITHUB_OUTPUT
            echo "sync_reason=hash_no_changes" >> $GITHUB_OUTPUT
          else
            echo "哈希不同，内容有变化，需要同步"
            echo "need_sync=true" >> $GITHUB_OUTPUT
            echo "sync_reason=hash_changed" >> $GITHUB_OUTPUT
          fi

      - name: Sync folders from source
        id: sync_folders
        if: steps.decide_sync.outputs.need_sync == 'true'
        run: |
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          SYNC_REASON="${{ steps.decide_sync.outputs.sync_reason }}"
          
          echo "开始同步文件夹 (原因: $SYNC_REASON)..."
          
          # 清空 meta 目录（保留目录本身）
          rm -rf meta/*
          
          # 使用 rsync 同步，排除 .git 和 .github 目录
          rsync -av \
            --delete \
            --exclude='.git' \
            --exclude='.github' \
            "$SOURCE_DIR/" "meta/"
          
          echo "✅ 同步完成"
          
          # 显示同步后的结构
          echo "同步后的 meta 目录结构:"
          ls -la meta/
          
          # 统计文件夹数量
          FOLDER_COUNT=$(find meta -maxdepth 1 -type d 2>/dev/null | tail -n +2 | wc -l)
          echo "folder_count=$FOLDER_COUNT" >> $GITHUB_OUTPUT
          
          # 统计文件数量
          FILE_COUNT=$(find meta -type f 2>/dev/null | wc -l)
          echo "file_count=$FILE_COUNT" >> $GITHUB_OUTPUT

      - name: Remove any .dat files (just in case)
        if: steps.sync_folders.outputs.folder_count
        run: |
          echo "确保没有 .dat 文件（与源仓库完全一致）..."
          
          if find meta -name "*.dat" -type f 2>/dev/null | grep -q .; then
            echo "发现 .dat 文件，正在删除..."
            find meta -name "*.dat" -type f -delete
            echo "✅ 已删除 .dat 文件"
          else
            echo "✓ 没有 .dat 文件（正确）"
          fi

      - name: Check for git changes
        id: check_git
        run: |
          echo "检查 Git 状态变化..."
          
          # 添加所有变化
          git add -A
          
          # 检查是否有待提交的更改
          if git diff --cached --quiet; then
            echo "没有 Git 变化"
            echo "has_git_changes=false" >> $GITHUB_OUTPUT
          else
            CHANGED_COUNT=$(git diff --cached --name-only | wc -l)
            CHANGED_FILES=$(git diff --cached --name-only | head -5)
            echo "有 $CHANGED_COUNT 个文件变化"
            echo "has_git_changes=true" >> $GITHUB_OUTPUT
            echo "changed_count=$CHANGED_COUNT" >> $GITHUB_OUTPUT
            
            if [ $CHANGED_COUNT -gt 0 ]; then
              echo "前 5 个变化的文件:"
              echo "$CHANGED_FILES"
            fi
          fi

      - name: Commit and push changes
        if: |
          (steps.check_git.outputs.has_git_changes == 'true') ||
          (github.event.inputs.force_sync == 'true' && steps.decide_sync.outputs.need_sync == 'true')
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          CHANGED_COUNT="${{ steps.check_git.outputs.changed_count || 0 }}"
          SYNC_REASON="${{ steps.decide_sync.outputs.sync_reason }}"
          
          # 构建提交信息
          if [ "${{ github.event.inputs.force_sync }}" = "true" ]; then
            SYNC_TYPE="强制同步"
            if [ "$SYNC_REASON" = "force_sync_user_request" ]; then
              COMMIT_SUMMARY="🔧 强制同步（用户请求）"
              COMMIT_DETAIL="用户选择强制同步选项，即使哈希无变化也执行同步。"
            else
              COMMIT_SUMMARY="🔄 强制同步更新"
              COMMIT_DETAIL="用户选择强制同步选项，已更新所有文件。"
            fi
          elif [ "$SYNC_REASON" = "hash_changed" ]; then
            SYNC_TYPE="自动同步"
            COMMIT_SUMMARY="🔄 检测到哈希变化"
            COMMIT_DETAIL="检测到源仓库哈希变化，自动同步更新。"
          elif [ "$SYNC_REASON" = "empty_directory" ]; then
            SYNC_TYPE="初始化同步"
            COMMIT_SUMMARY="📁 初始化 meta 目录"
            COMMIT_DETAIL="首次同步或 meta 目录为空，从源仓库初始化。"
          else
            SYNC_TYPE="自动同步"
            COMMIT_SUMMARY="🔄 常规同步"
            COMMIT_DETAIL="按计划执行同步任务。"
          fi
          
          COMMIT_MSG="$COMMIT_SUMMARY

          $COMMIT_DETAIL

          详细信息:
          - 源仓库: MetaCubeX/meta-rules-dat@meta 分支
          - 同步时间: $TIMESTAMP
          - 同步类型: $SYNC_TYPE
          - 同步原因: $SYNC_REASON
          - 变化文件数: $CHANGED_COUNT
          - 文件夹数: ${{ steps.sync_folders.outputs.folder_count || 'N/A' }}
          - 文件总数: ${{ steps.sync_folders.outputs.file_count || steps.source_status.outputs.source_file_count || 'N/A' }}

          哈希状态:
          - 当前哈希: ${{ steps.current_status.outputs.current_hash || '空/不存在' }}
          - 源仓库哈希: ${{ steps.source_status.outputs.source_hash }}
          - 哈希比较: ${{ steps.current_status.outputs.current_hash == steps.source_status.outputs.source_hash && '相同' || '不同' }}

          此提交由 GitHub Actions 工作流自动创建。"

          echo "提交信息:"
          echo "$COMMIT_MSG"
          
          # 如果有变化才提交，或者强制同步模式且需要同步
          if [ "$CHANGED_COUNT" -gt 0 ] || [ "${{ github.event.inputs.force_sync }}" = "true" ]; then
            git commit -m "$COMMIT_MSG"
            git push origin main
            echo "✅ 已提交并推送更新"
          else
            echo "⚠️  没有文件变化，跳过提交"
          fi

      - name: No changes needed
        if: steps.decide_sync.outputs.need_sync == 'false'
        run: |
          echo "🎉 已是最新版本，无需同步"
          echo ""
          echo "哈希状态:"
          echo "- 当前哈希: ${{ steps.current_status.outputs.current_hash }}"
          echo "- 源仓库哈希: ${{ steps.source_status.outputs.source_hash }}"
          echo "- 比较结果: ✅ 哈希相同，内容一致"
          echo ""
          echo "文件统计:"
          echo "- 文件数量: ${{ steps.current_status.outputs.current_file_count || 0 }}"
          echo ""
          echo "与源仓库完全一致 ✓"
          echo ""
          echo "提示: 如需强制覆盖，可使用手动触发并勾选'强制同步（即使哈希无变化）'"
          echo "下次自动检查时间: UTC 6:30 和 18:30"

      - name: Cleanup temporary files
        if: always()
        run: |
          echo "清理临时文件..."
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          
          # 清理源仓库目录
          if [ -n "$SOURCE_DIR" ] && [ -d "$SOURCE_DIR" ]; then
            rm -rf "$SOURCE_DIR"
            echo "✅ 已清理源仓库临时目录"
          fi
          
          # 清理其他临时文件
          rm -f /tmp/current_hash.txt /tmp/source_hash.txt 2>/dev/null || true

      - name: Final summary
        run: |
          echo "=== 同步任务完成 ==="
          echo "完成时间: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "触发方式: ${{ github.event_name }}"
          
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "手动触发参数:"
            echo "  - 强制同步（即使哈希无变化）: ${{ github.event.inputs.force_sync }}"
          fi
          
          echo ""
          echo "执行结果:"
          
          if [ "${{ steps.decide_sync.outputs.need_sync }}" = "false" ]; then
            echo "✅ 无需同步 - 哈希相同，内容已是最新"
          elif [ "${{ steps.decide_sync.outputs.sync_reason }}" = "force_sync_user_request" ]; then
            echo "🔧 强制同步执行 - 用户请求（哈希相同但强制覆盖）"
          elif [ "${{ steps.check_git.outputs.has_git_changes }}" = "true" ]; then
            echo "✅ 已同步并提交 - ${{ steps.check_git.outputs.changed_count }} 个文件更新"
          else
            echo "ℹ️  已同步但无需提交 - 没有检测到文件变化"
          fi
          
          echo ""
          echo "meta 目录状态:"
          if [ -d "meta" ]; then
            ls -la meta/
            echo ""
            echo "目录统计:"
            find meta -maxdepth 1 -type d 2>/dev/null | tail -n +2 | wc -l | xargs echo "📁 文件夹数:"
            find meta -type f 2>/dev/null | wc -l | xargs echo "📄 文件总数:"
          else
            echo "❌ meta 目录不存在"
          fi
          
          echo ""
          echo "哈希状态:"
          echo "- 当前哈希: ${{ steps.current_status.outputs.current_hash || '空/不存在' }}"
          echo "- 源仓库哈希: ${{ steps.source_status.outputs.source_hash }}"
          
          echo ""
          echo "=== 工作流执行结束 ==="
