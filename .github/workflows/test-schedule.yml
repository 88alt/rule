name: Quick Schedule Test

on:
  # 多种触发方式
  schedule:
    - cron: '* * * * *'   # 每分钟一次，快速测试
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Debug Info
        run: |
          echo "🎯 事件类型: ${{ github.event_name }}"
          echo "🕐 UTC时间: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "📍 本地时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "👤 触发者: ${{ github.actor }}"
          echo "📦 仓库: ${{ github.repository }}"
          
          # 记录到文件，便于查看
          echo "$(date) - ${{ github.event_name }} - ${{ github.actor }}" >> schedule_log.txt
          
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "✅ SCHEDULE事件触发成功！"
          else
            echo "手动触发或push触发"
          fi
